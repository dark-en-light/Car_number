public class Main {
    public static void main(String[] args) {
        NumberInfo numberInfo = new NumberInfo(); //Объявление класса "NumberInfo".
        numberInfo.getNumberInfo(); //Вызов метода "getNumberInfo()" из класса "NumberInfo".
    }
}
